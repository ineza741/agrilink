# AgriFeed Frontend

React frontend scaffold for the AI-Assisted Farmer Decision Support System.

## Getting started

1. Install dependencies with `npm install`
2. Start the dev server with `npm run dev`
